{% schema %}
  {
    "name": "Columns",
    "settings": [
      {
        "type": "checkbox",
        "id": "add_padding",
        "label": "Add Padding"
      }
    ],
    "blocks": [
      {
        "type": "block",
        "name": "Block",
        "settings": [
          {
            "type": "text",
            "id": "title",
            "label": "Title"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Columns"
      }
    ]
  }
{% endschema %}