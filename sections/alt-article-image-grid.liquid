{% render 'hs-defer-css' with filename: 'alt-article-content.css', pages: 'collection,index,product' | split: ',', local: true %}

{% assign images = article.metafields.custom.image_grid.value %}
{% if images != blank %}
    <div class="alt__article--content">
        <div class="article-container">
            <div class="image-grid">
                {% for image in images %}
                <div class="image-grid-item">{{ image.images }}</div>
                {% endfor %}            
            </div>
        </div>
    </div>
{% endif %}

{% schema %}
{
    "name": "Article Image Grid",
    "presets": [
        {
            "name": "Article Image Grid"
        }
    ]
}
{% endschema %}