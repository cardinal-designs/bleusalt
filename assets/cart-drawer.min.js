// class CartDrawer extends HTMLElement{constructor(){super(),this.addEventListener("keyup",e=>"Escape"===e.code&&this.close()),this.querySelector("#CartDrawer-Overlay").addEventListener("click",this.close.bind(this)),this.setHeaderCartIconAccessibility(),this.wearWithElements=document.querySelectorAll("wear-with")}setHeaderCartIconAccessibility(){let e=document.querySelector("#cart-icon-bubble");e.setAttribute("role","button"),e.setAttribute("aria-haspopup","dialog"),e.addEventListener("click",t=>{t.preventDefault(),this.open(e)}),e.addEventListener("keydown",t=>{"SPACE"===t.code.toUpperCase()&&(t.preventDefault(),this.open(e))})}open(e){e&&this.setActiveElement(e),(e=this.querySelector('[id^="Details-"] summary'))&&!e.hasAttribute("role")&&this.setSummaryAccessibility(e),setTimeout(()=>{this.classList.add("animate","active")}),this.addEventListener("transitionend",()=>{let e=document.getElementById("CartDrawer"),t=this.querySelector(".drawer__inner")||this.querySelector(".drawer__close");trapFocus(e,t)},{once:!0}),this.wearWith(),document.body.classList.add("overflow-hidden"),"object"==typeof BOLD&&BOLD.common&&BOLD.common.eventEmitter&&"function"==typeof BOLD.common.eventEmitter.emit&&BOLD.common.eventEmitter.emit("BOLD_COMMON_cart_loaded")}close(){this.classList.remove("active"),removeTrapFocus(this.activeElement),document.body.classList.remove("overflow-hidden")}setSummaryAccessibility(e){e.setAttribute("role","button"),e.setAttribute("aria-expanded","false"),e.nextElementSibling.getAttribute("id")&&e.setAttribute("aria-controls",e.nextElementSibling.id),e.addEventListener("click",e=>{e.currentTarget.setAttribute("aria-expanded",!e.currentTarget.closest("details").hasAttribute("open"))}),e.parentElement.addEventListener("keyup",onKeyUpEscape)}forceUpdateCartDrawer(){console.log("forceUpdateCartDrawer"),fetch("/cart").then(e=>e.text()).then(e=>{["cart-drawer .drawer__inner","#cart-icon-bubble"].forEach(t=>{let r=new DOMParser().parseFromString(e,"text/html"),i=document.querySelector(t);t=r.querySelector(t),i&&t&&(i.innerHTML=t.innerHTML)}),this.classList.contains("active")||this.open(),"object"==typeof BOLD&&BOLD.common&&BOLD.common.eventEmitter&&"function"==typeof BOLD.common.eventEmitter.emit&&BOLD.common.eventEmitter.emit("BOLD_COMMON_cart_loaded"),bundleUpdateCart()})}updateWearWith(e){e=new DOMParser().parseFromString(e,"text/html").querySelector(".wear-with__desktop");let t=document.querySelector(".wear-with__desktop");e&&t&&(t.innerHTML=e.innerHTML,this.wearWith())}renderContents(e){updateCart()}wearWith(e=!1){console.log("wearWith");let t=749<window.innerWidth?this.querySelector(".wear-with__desktop wear-with"):this.querySelector(".wear-with__mobile wear-with");if(t){var r=749<window.innerWidth?".wear-with__desktop":".wear-with__mobile",i=t.querySelector(".wear-with__swiper.swiper-initialized");i&&(console.log("swiper already initialized, destroy it bitch"),i.swiper.destroy()),i=t.getAttribute("data-url");var a=t.getAttribute("data-color"),n=t.getAttribute("data-product-id"),s=t.querySelector(".swiper-wrapper");if(s){s.innerHTML;var o=0;e&&(i="/collections/all?sort_by=best-selling&view=wear-with"),fetch(i).then(e=>e.text()).then(t=>{t=new DOMParser().parseFromString(t,"text/html").querySelectorAll(".card-product"),e&&0===t.length||(t.forEach(e=>{e.getAttribute("data-id")===n||"The Canvas Bag"===e.getAttribute("data-title")||"Gift wrapping"===e.getAttribute("data-title")||6<=o||(e.classList.add("swiper-slide"),s.appendChild(e),a&&(e=e.querySelector(`.quick-add__button--color[value="${a}"]`))&&(e.click(),e.closest(".quick-add__colors").style.display="none"),lazyImages(),o++)}),0===o&&this.wearWith(!0),t=749<window.innerWidth?3:2,749<window.innerWidth&&new Swiper(".wear-with__swiper",{slidesPerView:t,spaceBetween:20,allowTouchMove:!1,preventClicks:!0,preventClicksPropagation:!0,watchSlidesProgress:!0,loop:!1,navigation:{nextEl:`${r} .swiper-next`,prevEl:`${r} .swiper-prev`}}))}).catch(e=>console.log(e))}}}getSectionInnerHTML(e,t=".shopify-section"){return new DOMParser().parseFromString(e,"text/html").querySelector(t).innerHTML}getSectionsToRender(){return[{id:"cart-drawer",selector:"#CartDrawer"},{id:"cart-icon-bubble"}]}getSectionDOM(e,t=".shopify-section"){return new DOMParser().parseFromString(e,"text/html").querySelector(t)}setActiveElement(e){this.activeElement=e}}customElements.define("cart-drawer",CartDrawer);class CartDrawerItems extends CartItems{getSectionsToRender(){return[{id:"CartDrawer",section:"cart-drawer",selector:".drawer__inner"},{id:"cart-icon-bubble",section:"cart-icon-bubble",selector:".shopify-section"},]}}customElements.define("cart-drawer-items",CartDrawerItems);class BundleRemove extends HTMLElement{constructor(){super(),this.querySelector("button").addEventListener("click",this.onClick.bind(this)),this.cartDrawer=document.querySelector("cart-drawer")}onClick(){let e=this.getAttribute("data-bundle-items").split(","),t=this.getAttribute("data-bundle-items-key").split(","),r={updates:{},sections:"cart-drawer,cart-icon-bubble,main-cart-items,main-cart-footer",sections_url:window.location.pathname};t?t?.forEach(e=>{""!=e&&(r.updates[e?.trim()]=0)}):e.forEach(e=>{""!=e&&(r.updates[e]=0)}),fetch("/cart/update.js",{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/json"},body:JSON.stringify(r)}).then(e=>e.json()).then(e=>{this.cartDrawer.renderContents(e),this.cartDrawer.classList.contains("active")||this.cartDrawer.open(),setTimeout(function(){document.querySelector("cart-drawer").forceUpdateCartDrawer(),document.querySelector("cart-items").forceUpdateCartDrawer()},1e3),bundleUpdateCart()}).catch(e=>{console.error("Error:",e)})}}customElements.define("bundle-remove",BundleRemove);


class CartDrawer extends HTMLElement {
    constructor() {
        super(),
            this.addEventListener("keyup", (e) => "Escape" === e.code && this.close()),
            this.querySelector("#CartDrawer-Overlay").addEventListener("click", this.close.bind(this)),
            this.setHeaderCartIconAccessibility(),
            (this.wearWithElements = document.querySelectorAll("wear-with"));
    }
    setHeaderCartIconAccessibility() {
        let e = document.querySelector("#cart-icon-bubble");
        e.setAttribute("role", "button"),
            e.setAttribute("aria-haspopup", "dialog"),
            e.addEventListener("click", (t) => {
                t.preventDefault(), this.open(e);
            }),
            e.addEventListener("keydown", (t) => {
                "SPACE" === t.code.toUpperCase() && (t.preventDefault(), this.open(e));
            });
    }
    open(e) {
        e && this.setActiveElement(e),
            (e = this.querySelector('[id^="Details-"] summary')) && !e.hasAttribute("role") && this.setSummaryAccessibility(e),
            setTimeout(() => {
                this.classList.add("animate", "active");
            }),
            this.addEventListener(
                "transitionend",
                () => {
                    let e = document.getElementById("CartDrawer"),
                        t = this.querySelector(".drawer__inner") || this.querySelector(".drawer__close");
                    trapFocus(e, t);
                },
                { once: !0 }
            ),
            this.wearWith(),
            document.body.classList.add("overflow-hidden"),
            "object" == typeof BOLD && BOLD.common && BOLD.common.eventEmitter && "function" == typeof BOLD.common.eventEmitter.emit && BOLD.common.eventEmitter.emit("BOLD_COMMON_cart_loaded");
    }
    close() {
        this.classList.remove("active"), removeTrapFocus(this.activeElement), document.body.classList.remove("overflow-hidden");
    }
    setSummaryAccessibility(e) {
        e.setAttribute("role", "button"),
            e.setAttribute("aria-expanded", "false"),
            e.nextElementSibling.getAttribute("id") && e.setAttribute("aria-controls", e.nextElementSibling.id),
            e.addEventListener("click", (e) => {
                e.currentTarget.setAttribute("aria-expanded", !e.currentTarget.closest("details").hasAttribute("open"));
            }),
            e.parentElement.addEventListener("keyup", onKeyUpEscape);
    }
    forceUpdateCartDrawer() {
        console.log("forceUpdateCartDrawer"),
            fetch("/cart")
                .then((e) => e.text())
                .then((e) => {
                    ["cart-drawer .drawer__inner", "#cart-icon-bubble"].forEach((t) => {
                        let r = new DOMParser().parseFromString(e, "text/html"),
                            i = document.querySelector(t);
                        (t = r.querySelector(t)), i && t && (i.innerHTML = t.innerHTML);
                    }),
                        this.classList.contains("active") || this.open(),
                        "object" == typeof BOLD && BOLD.common && BOLD.common.eventEmitter && "function" == typeof BOLD.common.eventEmitter.emit && BOLD.common.eventEmitter.emit("BOLD_COMMON_cart_loaded"),
                        bundleUpdateCart();
                });
    }
    updateWearWith(e) {
        e = new DOMParser().parseFromString(e, "text/html").querySelector(".wear-with__desktop");
        let t = document.querySelector(".wear-with__desktop");
        e && t && ((t.innerHTML = e.innerHTML), this.wearWith());
    }
    renderContents(e) {
        updateCart();
    }

    // quickAddClick(cardElement, event) {
    //   console.log("Click on quick add button in:", cardElement);
    //   console.log("Button clicked:", event.currentTarget);

    //   let selectedColor = cardElement.querySelector('.quick-add__button--color[selected]');

    //   console.log("selectedColor", selectedColor)
    

    
    //   let cardJson = JSON.parse(cardElement.getAttribute("data-json"));
    //   let cardVariants = cardJson.variants;
    
    //   console.log("cardVariants", cardVariants);
      
    //   cardElement.querySelectorAll('.quick-add__add-button').forEach(
    //     (button) => button.removeAttribute('selected')
    //   );
    //   cardElement.querySelectorAll('.quick-add__button[selected]:not(.quick-add__button--color)').forEach(
    //     (button) => button.removeAttribute('selected')
    //   );
      
    //   event.target.setAttribute('selected', true);
    //   let allSelected = [];
    //   // quick-add__button quick-add__button--size
    //   cardElement.querySelectorAll('.quick-add__add-button[selected]').forEach((button) => {
    //     if(button.value != "color" && button.value != "Color"){
    //       allSelected.push(button.value);
    //     }
    //   });
    //   cardElement.querySelectorAll('.quick-add__button[selected]:not(.quick-add__button--color)').forEach((button) => {
    //       if (button.value !== "color" && button.value !== "Color") {
    //           allSelected.push(button.value);
    //       }
    //   });

      
    //   const colorButtons = cardElement.querySelectorAll('.quick-add__button--color[selected]');
    //   if (colorButtons?.length > 0) {
    //     const lastColorButton = colorButtons[colorButtons.length - 1]; // Get the last selected color button
    //     allSelected.push(lastColorButton.value);
    //   }


    //   console.log("allSelected", allSelected)

    //   let selectedArray = allSelected?.length > 1 ? allSelected.reverse().join(' / ') : allSelected[0];

    //   console.log("selectedArray", selectedArray)

      

    //   cardVariants.forEach((variant) => {
    //     console.log("variant.title", variant.title)
    //     // console.log("selectedArray", selectedArray);
        
    //     if (variant.title === selectedArray) {
    //       console.log("variant", variant)

    //       console.log("calling function here")
    //       // addToCart(variant.id, 1);
    //       return false;
    //     }
    //   });
    // }

  quickAddClick(cardElement, event) {
    // console.log("Click on quick add button in:", cardElement);
    
    console.log("Button clicked:", event.currentTarget);

    
  
    let selectedColor = cardElement.querySelector('.quick-add__button--color[selected]');
  
    console.log("selectedColor", selectedColor);
  
    let cardJson = JSON.parse(cardElement.getAttribute("data-json"));
    console.log("cardJson", cardJson)
    let cardVariants = cardJson.variants;
  
    // console.log("cardVariants", cardVariants);
  
    cardElement.querySelectorAll('.quick-add__add-button').forEach(
      (button) => button.removeAttribute('selected')
    );
  
    cardElement.querySelectorAll('.quick-add__button[selected]:not(.quick-add__button--color)').forEach(
      (button) => button.removeAttribute('selected')
    );
  
    event.target.setAttribute('selected', true);
  
    let allSelected = [];
  
    // Collect selected button values
    cardElement.querySelectorAll('.quick-add__add-button[selected]').forEach((button) => {
      if (button.value != "color" && button.value != "Color") {
        allSelected.push(button.value);
      }
    });
  
    cardElement.querySelectorAll('.quick-add__button[selected]:not(.quick-add__button--color)').forEach((button) => {
      if (button.value !== "color" && button.value !== "Color") {
        allSelected.push(button.value);
      }
    });
  
    const colorButtons = cardElement.querySelectorAll('.quick-add__button--color[selected]');
    if (colorButtons?.length > 0) {
      const lastColorButton = colorButtons[colorButtons.length - 1]; // Get the last selected color button
      allSelected.push(lastColorButton.value);
    }
  
    // console.log("allSelected", allSelected);
  
    let selectedArray = allSelected?.length > 1 ? allSelected.reverse().join(' / ') : allSelected[0];
  
    // console.log("selectedArray", selectedArray);
  
    // Now we check and add to the cart only if there's a match with the selected variant

    // let colorIndex = cardJson.options?.indexOf("Color") || cardJson.options.findIndex(option => option.name == 'Color');

    let colorIndex = cardJson.options?.indexOf("Color");
    if (colorIndex === -1 || colorIndex === undefined) {
      colorIndex = cardJson.options?.findIndex(option => option.name === "Color");
    }

    let sizeIndex = cardJson.options?.indexOf("Size");
    if (sizeIndex === -1 || sizeIndex === undefined) {
      sizeIndex = cardJson.options?.findIndex(option => option.name === "Size");
    }


    console.log("selectedColor", selectedColor)
    console.log("colorIndex", colorIndex)
    console.log("sizeIndex", sizeIndex)
    
    if (!selectedColor || colorIndex === -1 || sizeIndex === -1) return;

  
    cardVariants.forEach((variant) => {
      let colorOption = variant.options?.[colorIndex];
      let sizeOption = variant.options?.[sizeIndex];
  
      if (colorOption !== selectedColor.value || colorOption !== selectedColor.value) return;

      console.log("sizeOption", sizeOption)
      if (typeof sizeOption === 'undefined') return;
  
      let sizeButton = cardElement.querySelector(`button[value="${sizeOption}"]`);
      if (sizeButton) sizeButton.disabled = !variant.available;
    });

    
    if (event.target.classList.contains("quick-add__button--color")) {
      return;
    }
      
    let matchFound = false;
    cardVariants.forEach((variant) => {
      // console.log("variant.title", variant.title);
      if (variant.title === selectedArray) {
        console.log("variant:", variant);
        addToCart(variant.id, 1); // Add to cart only when there's a match
        matchFound = true;
        return false; // Stop the loop once a match is found
      }
    });
  
    
  }



    quickAddIntialize(cards) {
      if (window.location.href.includes("/products/")) {
        cards.forEach((element) => {
          element.querySelectorAll('.quick-add__add-button').forEach((button) => {
            button.addEventListener('click', this.quickAddClick.bind(this, element));
          });
          element.querySelectorAll('.quick-add__button').forEach((button) => {
            button.addEventListener('click', this.quickAddClick.bind(this, element));
          });
        });
        
      }
    }

    wearWith(e = !1) {
        console.log("wearWith");
        let t = 749 < window.innerWidth ? this.querySelector(".wear-with__desktop wear-with") : this.querySelector(".wear-with__mobile wear-with");
        if (t) {
            var r = 749 < window.innerWidth ? ".wear-with__desktop" : ".wear-with__mobile",
                i = t.querySelector(".wear-with__swiper.swiper-initialized");
            i && (console.log("swiper already initialized, destroy it bitch"), i.swiper.destroy()), (i = t.getAttribute("data-url"));
            var a = t.getAttribute("data-color"),
                n = t.getAttribute("data-product-id"),
                s = t.querySelector(".swiper-wrapper");
            if (s) {
                s.innerHTML;
                var o = 0;
                e && (i = "/collections/all?sort_by=best-selling&view=wear-with"),
                    fetch(i)
                        .then((e) => e.text())
                        .then((t) => {
                            (t = new DOMParser().parseFromString(t, "text/html").querySelectorAll(".card-product")),
                                this.quickAddIntialize(t),
                                (e && 0 === t.length) ||
                                    (t.forEach((e) => {
                                        e.getAttribute("data-id") === n ||
                                            "The Canvas Bag" === e.getAttribute("data-title") ||
                                            "Gift wrapping" === e.getAttribute("data-title") ||
                                            6 <= o ||
                                            (e.classList.add("swiper-slide"),
                                            s.appendChild(e),
                                            a && (e = e.querySelector(`.quick-add__button--color[value="${a}"]`)) && (e.click(), (e.closest(".quick-add__colors").style.display = "none")),
                                            lazyImages(),
                                            o++);
                                    }),
                                    0 === o && this.wearWith(!0),
                                    (t = 749 < window.innerWidth ? 3 : 2),
                                    749 < window.innerWidth &&
                                        new Swiper(".wear-with__swiper", {
                                            slidesPerView: t,
                                            spaceBetween: 20,
                                            allowTouchMove: !1,
                                            preventClicks: !0,
                                            preventClicksPropagation: !0,
                                            watchSlidesProgress: !0,
                                            loop: !1,
                                            navigation: { nextEl: `${r} .swiper-next`, prevEl: `${r} .swiper-prev` },
                                        }));
                          
                        })
                        .catch((e) => console.log(e));
            }
        }
    }
    getSectionInnerHTML(e, t = ".shopify-section") {
        return new DOMParser().parseFromString(e, "text/html").querySelector(t).innerHTML;
    }
    getSectionsToRender() {
        return [{ id: "cart-drawer", selector: "#CartDrawer" }, { id: "cart-icon-bubble" }];
    }
    getSectionDOM(e, t = ".shopify-section") {
        return new DOMParser().parseFromString(e, "text/html").querySelector(t);
    }
    setActiveElement(e) {
        this.activeElement = e;
    }
}
customElements.define("cart-drawer", CartDrawer);
class CartDrawerItems extends CartItems {
    getSectionsToRender() {
        return [
            { id: "CartDrawer", section: "cart-drawer", selector: ".drawer__inner" },
            { id: "cart-icon-bubble", section: "cart-icon-bubble", selector: ".shopify-section" },
        ];
    }
}
customElements.define("cart-drawer-items", CartDrawerItems);
class BundleRemove extends HTMLElement {
    constructor() {
        super(), this.querySelector("button").addEventListener("click", this.onClick.bind(this)), (this.cartDrawer = document.querySelector("cart-drawer"));
    }
    onClick() {
        let e = this.getAttribute("data-bundle-items").split(","),
            t = this.getAttribute("data-bundle-items-key").split(","),
            r = { updates: {}, sections: "cart-drawer,cart-icon-bubble,main-cart-items,main-cart-footer", sections_url: window.location.pathname };
        t
            ? t?.forEach((e) => {
                  "" != e && (r.updates[e?.trim()] = 0);
              })
            : e.forEach((e) => {
                  "" != e && (r.updates[e] = 0);
              }),
            fetch("/cart/update.js", { method: "POST", credentials: "same-origin", headers: { "Content-Type": "application/json" }, body: JSON.stringify(r) })
                .then((e) => e.json())
                .then((e) => {
                    this.cartDrawer.renderContents(e),
                        this.cartDrawer.classList.contains("active") || this.cartDrawer.open(),
                        setTimeout(function () {
                            document.querySelector("cart-drawer").forceUpdateCartDrawer(), document.querySelector("cart-items").forceUpdateCartDrawer();
                        }, 1e3),
                        bundleUpdateCart();
                })
                .catch((e) => {
                    console.error("Error:", e);
                });
    }
}
customElements.define("bundle-remove", BundleRemove);
